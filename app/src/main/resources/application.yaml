server:
  port: 8080

spring:
  profiles:
    active: local
  threads:
    virtual:
      enabled: true
  jackson:
    property-naming-strategy: SNAKE_CASE
    
otp:
  expiration-minutes: 5
  max-attempts: 3
  code-length: 6

logging:
  level:
    com.fabiankevin.app: INFO